<?xml version="1.0" encoding="utf-8"?>
<!--
    Debug-only network security config.

    Allows cleartext HTTP to the loopback / emulator-host aliases so the
    Flutter app can reach the local FastAPI AI backend during development.

    - 10.0.2.2  : standard Android Studio AVD alias for the host machine
    - localhost / 127.0.0.1 : used together with `adb reverse tcp:8000 tcp:8000`
                              (the recommended tunnel in the project README)

    Release builds do NOT include this file, so HTTPS stays required there.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>